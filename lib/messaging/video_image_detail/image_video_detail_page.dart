import 'package:flutter/material.dart';

class ImageVideoDetailPage extends StatelessWidget {
  const ImageVideoDetailPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Text('teri teri'),
    );
  }
}
